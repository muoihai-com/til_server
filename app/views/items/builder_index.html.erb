<h1 class="title"><a href="/">Today I Learned</a></h1>

<div class="container">
  <div class="items">
    <% Item.all.each do |item| %>
      <div class="item">
        <h2 class="title">
          <%= item.title %>
        </h2>
        <div class="content">
          <%= markdown(item.content) %>
        </div>
      </div>
    <% end %>
  </div>
</div>
